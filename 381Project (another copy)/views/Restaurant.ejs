<!DOCTYPE html>
<html lang="en"><head>
<meta http-equiv="content-type" content="text/html; charset=UTF-8">
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>Restaurant</title>
  <!-- Latest compiled and minified CSS -->
  <link rel="stylesheet" href="Restaurant_files/bootstrap.css">

  <!-- jQuery library -->
  <script src="Restaurant_files/jquery.js"></script>

  <!-- Latest compiled JavaScript -->
  <script src="Restaurant_files/bootstrap.js"></script>
<style type="text/css">
  .well {
      background: none;
      height: 320px;
  }

  .table-scroll tbody {
      position: absolute;
      overflow-y: scroll;
      height: 250px;
  }

  .table-scroll tr {
      width: 100%;
      table-layout: fixed;
      display: inline-table;
  }

  .table-scroll thead > tr > th {
      border: none;
  }
</style></head>


<body>
  <div class="jumbotron text-center container=fluid">
    <h1>Restaurants</h1>
    
      <p>User:<%= userid%></p>
      
    <p>Showing <%=restaurants.length%> documents 
    (criteria: <%= JSON.stringify(criteria)%>)</p>
<form action="/search">
    <label for="type">Search By:</label>
    <select id="type" name="type">
      <option value="borough">borough</option>
      <option value="name">Name</option>
      <option value="cuisine">cuisine</option>
    </select>
    <input type="search" id="value" name="value">
    <input type="submit">
  </form>

  </div>
  <div class="container-fluid">
    <div class="row">
      <a href="/new">
        <button class="btn btn-default">Create New Restaurant</button>
      </a>
    </div>
    <div class="row">
      <%if(restaurants.length> 0){ %>
      <table class="table table-striped table-bordered table-hover table-scroll">
        <thead>
          <tr>
            <th>Name</th>
          </tr>
        </thead>
 	 
        <tbody>
          <%for(var i= 0; i< restaurants.length; i++) { %>
            <tr>
              <td class=""><a href="/display?_id=<%=restaurants[i]._id%>">
			<%=restaurants[i].name%></a></td>
            </tr>
            <%} %>
        </tbody>
      </table>
	<%} else{ %>
		<h1>Nothing to Show!</h1>
	<%} %>
    </div>
  </div>
    

</body></html>
